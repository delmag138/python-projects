"""
Ask the user for a word.

Print "Yes, it’s a palindrome" if the word reads the same forwards and backwards.

Example: madam → palindrome.
"""

def palindrome(user_input):
    if user_input == user_input[::-1]:
        print("Hurray, it's a palindrome word!")
    else:
        print("Nope, better luck next time!")

palindrome(input("Enter a palindrome word:"))
